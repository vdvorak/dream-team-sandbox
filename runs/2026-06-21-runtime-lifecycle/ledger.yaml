---
run: 2026-06-21-runtime-lifecycle
node: intake
outcome: PASS
class: feature
note: Implementace runtime control-plane (páteř) — lifecycle slice (healthz + ensure/get/sleep/destroy
  + Environment FSM + fail-closed ZEĎ provider). Net-new standalone, NE cross-repo
  extrakce.
agent: null
phase: null
time:
  seconds: 0
---
run: 2026-06-21-runtime-lifecycle
node: product
outcome: PASS
changed:
- spec
- acceptance
flags:
  has_ui: false
  touches_db: false
  touches_server: true
  touches_shared_ui: false
  has_deploy: false
tier: M
note: Slice 1 control-plane spec + acceptance (specs/runtime-control-plane.md, acceptance/runtime-control-plane.md).
  In-scope AC-1,2,3,4,5,9,10,11,12,13,14; deferred AC-6/7/8 (git/files/terminal).
  4 edge-case otázky vyřešeny orchestrátorem (ensure-on-destroyed=fresh per kontrakt
  §2).
outputs:
- type: spec
  path: specs/runtime-control-plane.md
- type: acceptance
  path: acceptance/runtime-control-plane.md
agent: vision-po
phase: T1
time:
  seconds: 0
---
run: 2026-06-21-runtime-lifecycle
node: spec-gate
outcome: FAIL
severity: blocking
signature: 'specs/runtime-control-plane.md — 4 druhy porušení agnostiky (spec musí
  mluvit doménově, ne implementačně/kontraktně):

  1) ř.12: přímá cesta `contracts/runtime-contract.md` → přeformulovat na "implementuje
  slice 1 runtime kontraktu (v1.0.0)".

  2) ř.27,30,31,32,40,62-64: HTTP metody/cesty (POST ensure, GET {project_id}, /v1/healthz,
  …/git/files/terminal) → doménové akce (zajisti/stav/uspi/zruš prostředí; zdravotní
  kontrola).

  3) ř.35,55,95: HTTP/error kódy v textu (502 ERR_PROVISION_FAILED, 401 ERR_UNAUTHORIZED,
  503 ERR_RUNTIME_UNAVAILABLE) → "server vrátí chybu (fail-closed)" / "selhání ověření"
  / "server nedosažitelný".

  4) ř.40-41: operationId (ensureEnvironment/getEnvironment/sleepEnvironment/destroyEnvironment)
  → doménové pojmy "čtyři lifecycle operace: zajisti/stav/uspi/zruš prostředí".

  Acceptance soubor je OK (HTTP/error kódy tam patří). Oprav JEN spec.

  '
note: spec-cleanliness BLOCKER ×4 (agnostika plochy); acceptance/konzistence/brevity
  OK.
agent: sheldon-spec
phase: T1
time:
  seconds: 0
---
run: 2026-06-21-runtime-lifecycle
node: product
outcome: PASS
changed:
- spec
flags:
  has_ui: false
  touches_db: false
  touches_server: true
  touches_shared_ui: false
  has_deploy: false
tier: S
note: Re-flow po spec-gate FAIL — spec přepsán do doménového jazyka (odstraněny HTTP
  metody/cesty, status/error kódy, operationId, file-path). Acceptance nedotčen.
outputs:
- type: spec
  path: specs/runtime-control-plane.md
- type: acceptance
  path: acceptance/runtime-control-plane.md
agent: vision-po
phase: T1
time:
  seconds: 0
---
run: 2026-06-21-runtime-lifecycle
node: spec-gate
outcome: FAIL
severity: blocking
signature: 'specs/runtime-control-plane.md — zbývá 6 výskytů agnostika-leaku (původní
  4 kategorie OPRAVENY). KOMPLETNÍ scrub, ne po položkách:

  - ř.5 note: "git/files/terminal" → "repozitář/soubory/terminál" nebo "AC-6/7/8".

  - ř.64 Scope Out: závorka "(git clone uvnitř kontejneru/workspace)" → smazat, nech
  jen "klonování repozitáře".

  - ř.71: "(název a tvar implementace jsou rozhodnutím CTO/architekta)" → smazat (role-reference;
  patří do handoffu, ne spec).

  - ř.78: "volá se z `healthz`" → "volá se z operace zdravotní kontroly".

  - ř.80-81: schema pole `control_url`/`terminal_url` → "opaque connection handle
  (adresy v connection objektu)".

  - ř.91: `2xx` → "úspěšná odpověď s aktuálním stavem".

  - ř.95-96: `http://localhost:9999` + "AC-11 grep test" → "placeholder adresa bez
  substrát-nounu" (vynech konkrétní URL i test-instrukci).

  NAVÍC projeď CELÝ spec řádek po řádku a odstraň JAKÝKOLI zbylý: HTTP sloveso/cesta/status/třída,
  error kód, název pole schématu, endpoint token, tool/command name, role/agent reference,
  file-path. Tohle musí být POSLEDNÍ cleanup — žádný další výskyt téhož druhu.

  '
note: spec-cleanliness FAIL
agent: sheldon-spec
phase: T1
time:
  seconds: 0
---
run: 2026-06-21-runtime-lifecycle
node: product
outcome: PASS
changed:
- spec
flags:
  has_ui: false
  touches_db: false
  touches_server: true
  touches_shared_ui: false
  has_deploy: false
tier: S
note: Re-flow
outputs:
- type: spec
  path: specs/runtime-control-plane.md
- type: acceptance
  path: acceptance/runtime-control-plane.md
agent: vision-po
phase: T1
time:
  seconds: 0
---
run: 2026-06-21-runtime-lifecycle
node: spec-gate
outcome: FAIL
severity: blocking
signature: 'STRUKTURÁLNÍ fix (ne další kosmetika): spec má být ČISTĚ BEHAVIORÁLNÍ.
  Vyhodit VŠECHNO "jak" — to patří downstream (architecture/contract/rules/stack),
  ne do product specu:

  - Auth mechanismus (mTLS / service token) → smazat; behaviorálně: "klient se autentizuje;
  neověřená identita → odmítnuto". (Mechanismus žije v kontraktu §3.)

  - Datová pole volání (project_id, repo.url, tool, phase, contract_version, connection,
  Environment jako typ) → smazat schema-tvary; behaviorálně: "zajisti prostředí pro
  projekt ze zdrojového repozitáře".

  - Storage/concurrency mechanika (in-memory store, serializace) → smazat; behaviorálně:
  "souběžné zajištění → jediné prostředí (idempotentně); stav přežívá mezi voláními".

  - Stub/dev provider jako test-double → behaviorálně: "enforcement je delegován na
  zaměnitelný provider; lokální varianta nevynucuje reálný enforcement".

  - CELÁ sekce §Enforcement-provider rozhraní s funkčními signaturami provision()/teardown()/sleep()/health()
  → SMAZAT ze specu. Návrh tohoto rozhraní je práce uzlu `architecture` (ted-architect),
  ne product specu. Spec smí jen: "server deleguje enforcement na zaměnitelný provider;
  provider garantuje aktivní enforcement nebo vrátí chybu; klient dostane connection
  handle JEN když je enforcement aktivní (fail-closed)".

  Výsledek = krátký behaviorální dokument (operace jako doménové akce, stavový automat,
  idempotence, fail-closed garance, standalone). Žádné signatury, schémata, mechaniky.

  '
note: spec-cleanliness FAIL
agent: sheldon-spec
phase: T1
time:
  seconds: 0
---
intervened: spec-gate->product
kind: resolve-loop
prev_count: 3
at: '2026-06-21T18:24:45.694413+00:00'
---
run: 2026-06-21-runtime-lifecycle
node: product
outcome: PASS
changed:
- spec
flags:
  has_ui: false
  touches_db: false
  touches_server: true
  touches_shared_ui: false
  has_deploy: false
tier: S
note: Definitivní lean behaviorální přepis (89 ř.) — celá §Enforcement-provider/auth/storage/schémata
  vyhozeny (přesun do architecture/contract/stack). Orchestrátor ověřil grepem CLEAN
  + obsahově kompletní.
outputs:
- type: spec
  path: specs/runtime-control-plane.md
- type: acceptance
  path: acceptance/runtime-control-plane.md
agent: vision-po
phase: T1
time:
  seconds: 0
---
run: 2026-06-21-runtime-lifecycle
node: spec-gate
outcome: PASS
note: Finální PASS — spec čistě behaviorální (89 ř.), agnostická, strukturálně kompletní.
  Po 3 audit-defect kolech + resolve-loop + strukturální lean přepis.
outputs:
- type: gate-output
agent: sheldon-spec
phase: T1
time:
  seconds: 0
---
run: 2026-06-21-runtime-lifecycle
node: feasibility
outcome: PASS
note: 'Feasible. Stack = Python 3.12 + FastAPI >=0.115 + Pydantic v2 + uvicorn + pytest-asyncio
  (zapsáno stack/runtime-control-plane.md), žádný nový dep, has_db=false drží.

  Rizika pro architecture/backend — A) tool validace musí vrátit 400 ERR_TOOL_NOT_ALLOWED,
  ne 422 (allowlist v service vrstvě, ne Pydantic enum); B) error envelope {code,message,detail},
  ne scaffold {code,details}; C) OpenAPI drift anyOf vs oneOf tolerovat (kontrakt
  = autorita); D) additionalProperties false i na vnořeném repo objektu (bypass přes
  repo.firewall); E) dev-provider connection URL bez substrát-nounu a bez portu 808x.

  Model: architecture=opus, backend=sonnet, qa=sonnet (defaulty).

  '
outputs:
- type: gate-output
agent: tony-cto
phase: T1
time:
  seconds: 0
---
run: 2026-06-21-runtime-lifecycle
node: architecture
outcome: PASS
changed:
- reuse-decision
flags:
  touches_db: false
  touches_server: true
  touches_shared_ui: false
note: 'Kontrakt v1.0.0 NEZMĚNĚN (ověřeno konzistentní, app-facing registr 10 kódů
  = §8 1:1). Zapsáno rules/runtime-control-plane.md (RCP-A1..A8) + stack Extraction
  Candidates.

  Klíčová rozhodnutí: EnforcementProvider Protocol s uzavřeným součtovým typem EnforcementActive|EnforcementFailed
  (fail-closed v typu); jediný fail-closed bod v service (ready jen po EnforcementActive);
  error nepropustnost překládá service dle kind nikoli internal_code; vrstvy Router->Service->Repository,
  in-memory repo (destroyed se nemaže), asyncio.Lock per project_id; DevEnforcementProvider
  (default) + CageEnforcementProvider (stub, napojení na server/cage OUT).

  Reuse: scaffold-only vrstvení + reuse cage errors read-only; feature-local error
  envelope + provider. A-E vyřešeno (tool allowlist v service->400; envelope {code,message,detail};
  anyOf~oneOf tolerováno; extra=forbid i na repo; dev URL bez substrát-nounu/808x).

  '
outputs:
- type: reuse-decision
  path: rules/runtime-control-plane.md
agent: ted-architect
phase: T2
time:
  seconds: 0
